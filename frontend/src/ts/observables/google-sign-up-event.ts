/**
 * @deprecated Google sign up removed in privacy fork
 * This file is a stub to prevent build errors.
 */

export function subscribe(
  _callback: (_user: unknown, _isNewUser: boolean) => void,
): void {
  // No-op - google sign up removed in privacy fork
}

export function unsubscribe(): void {
  // No-op - google sign up removed in privacy fork
}
